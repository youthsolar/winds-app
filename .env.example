# Google OAuth (Jeffery to provide after creating Cloud Console app)
PUBLIC_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Cloudflare D1 (runtime binding, set in wrangler.toml)
# DB binding configured in wrangler.toml, not here

# Worker API
PUBLIC_WORKER_API=https://api.winds.tw
